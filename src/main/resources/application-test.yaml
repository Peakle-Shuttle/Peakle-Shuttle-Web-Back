# ===========================================
# 로컬 개발 환경 설정
# ===========================================

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/peakle-shuttle?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&permitMysqlScheme=true&restrictedAuth=mysql_native_password
    username: root
    password: root123!

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

jwt:
  secret: peakle-shuttle-jwt-secret-key-minimum-32-characters-long
  access-token-validity: 3600000
  refresh-token-validity: 604800000
  access-header: Authorization
  grant-type: Bearer

app:
  cors:
    allowed-origins: http://localhost:5173
  oauth2:
    redirect-uri: http://localhost:5173/oauth2/redirect

client:
  kakao:
    name: kakao-auth-client
    client-id: 6d69581d11613143b5b4f00eceeb7d89
    public-key-url: https://kauth.kakao.com/.well-known/jwks.json
